# Установка базового образа
FROM golang:latest

# Установка рабочей директории
WORKDIR /app

# Копирование файлов проекта в контейнер
COPY . .

# Определение порта, на котором будет работать веб-сервер
EXPOSE 8088

# Команда для запуска веб-сервера
CMD ["./server"]